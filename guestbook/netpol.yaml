apiVersion: projectcalico.org/v3
kind: NetworkPolicy
metadata:
  name: argocd-test
  namespace: guestbook
spec:
  selector: all()
  ingress:
    - action: Allow
      source: {}
      destination: {}
  types:
    - Ingress
